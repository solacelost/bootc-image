#!/bin/bash

REGISTRY_LOGIN="${REGISTRY_LOGIN:-registry.jharmison.com}"

if ! oc whoami >/dev/null 2>&1; then
    echo "Not logged into any cluster" 2>dev/null
    exit 1
fi

podman login "${REGISTRY_LOGIN}" -u $(oc whoami) -p $(oc whoami -t)
