# config for monitors
exec bash -c 'echo single > /run/user/1000/monitorswap'
exec_always --no-startup-id "sh -c 'sleep 1 ; /usr/local/bin/monitorswap reload'"
bindsym $mod+Shift+m exec /usr/local/bin/monitorswap
